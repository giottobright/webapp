# 🚀 Быстрое исправление ошибки git clone

## ❌ Ошибка:
```
error: RPC failed; curl 56 GnuTLS recv error (-54)
fatal: fetch-pack: invalid index-pack output
```

## ✅ Решение:

### Вариант 1: Использовать Dockerfile.retry (РЕКОМЕНДУЕТСЯ)

**В Timeweb Cloud:**
1. Откройте настройки проекта webapp
2. В поле "Dockerfile path" укажите: `Dockerfile.retry`
3. Сохраните и пересоберите

**Или переименуйте локально:**
```bash
cd webapp
git mv Dockerfile.retry Dockerfile
git add Dockerfile
git commit -m "fix: улучшен Dockerfile с retry для git clone"
git push
```

### Вариант 2: Использовать стандартный Dockerfile

Просто используйте `Dockerfile` - он уже включает улучшения.

## 📋 Что исправлено:

✅ **5 попыток клонирования** (вместо 1)  
✅ **Задержка 10 секунд** между попытками  
✅ **Увеличенный таймаут**: 600 секунд  
✅ **Shallow clone** (--depth 1) - быстрее  
✅ **Автоматическая очистка** при неудаче  

## 🎯 После применения:

1. Пересоберите образ в Timeweb Cloud
2. Проверьте логи - должно быть "Clone successful!"
3. Если все еще падает - проверьте доступность GitHub

---

**Готово!** 🎉

